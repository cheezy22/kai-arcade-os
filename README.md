# Kai Arcade OS

Kai Arcade OS is an offline-first family arcade cabinet for iPhone, iPad and laptop browsers. Double-click `index.html` to play — no build step or network connection is required.

## Included in this starter

- Cabinet-style dashboard with original generated hero art
- Local player profile, XP-ready level, wins, recent activity and Teddy Mode preference
- Shared audio feedback, pause-safe lifecycle, visibility pause and lightweight animation loop
- Playable Pool break shot, Bowling, Darts, Connect Four and Retro World of Snacks rooms
- Modular room registry for the wider 20-game v1 catalog
- Touch-friendly buttons, responsive layout and no hover-only controls

## Run

Open `index.html`. The save file is stored in browser `localStorage` under `kai-arcade-save`.

## GitHub

```powershell
git init
git add .
git commit -m "Create Kai Arcade OS 1.5 offline arcade"
git branch -M main
git remote add origin https://github.com/YOUR-NAME/kai-arcade-os.git
git push -u origin main
```
