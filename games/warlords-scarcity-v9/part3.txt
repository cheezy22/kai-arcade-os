c.dipClock-=dt;if(c.dipClock<=0){c.dipClock=R(7,15);let others=clans.filter(o=>o.id!==c.id),o=pick(others),g=c.grudge[o.id]||0;if(c.war.has(o.id)){if(c.morale<35||Math.random()<.12){setWar(c.id,o.id,false);say(`🤝 ${c.name} and ${o.name} sign an uneasy peace.`)}}else if(g>22||c.rage>72||c.trait==='chaotic'&&Math.random()<.25){setWar(c.id,o.id,true,g>22?'an old blood debt':'pure bad judgement')}else if(g<5&&Math.random()<.14){c.allies.add(o.id);o.allies.add(c.id);say(`🤝 ${c.name} and ${o.name} form a snack pact.`)}}}
function updateTerritory(){for(const c of clans)c.territory=0;for(let gy=0;gy<ROWS;gy++)for(let gx=0;gx<COLS;gx++){let x=gx*CELL+CELL/2,y=gy*CELL+CELL/2,best=null,score=-1e9;for(const c of clans){let s=-Math.hypot(x-c.home.x,y-c.home.y);for(const b of buildings)if(b.c===c.id&&b.hp>0)s=Math.max(s,180-Math.hypot(x-b.x,y-b.y));for(const a of agents)if(a.alive&&a.c===c.id)s=Math.max(s,75-Math.hypot(x-a.x,y-a.y));if(s>score){score=s;best=c}}if(best&&score>-260)best.territory++}}
function battleDetect(){for(let i=0;i<agents.length;i++){let a=agents[i];if(!a.alive)continue;let e=nearestEnemy(a,70);if(e&&Math.random()<.025){battles.push({x:(a.x+e.x)/2,y:(a.y+e.y)/2,a:a.c,b:e.c,life:5,max:5});if(director&&Math.random()<.35){cam.tx=(a.x+e.x)/2;cam.ty=(a.y+e.y)/2;cam.tz=Math.max(cam.z,1.15);say(`🎥 Border clash: ${clans[a.c].name} vs ${clans[e.c].name}!`)}}}battles=battles.filter(b=>(b.life-=.3)>0)}
function randomEvent(){let c=pick(clans);if(Math.random()<.45){c.food+=35;say(`🍟 A legendary snack cache is found by ${c.name}.`)}else if(Math.random()<.5){c.morale=clamp(c.morale+18,0,100);say(`🎉 ${c.name} invents a national holiday for crisps.`)}else{let o=pick(clans.filter(x=>x!==c));c.grudge[o.id]=(c.grudge[o.id]||0)+12;say(`😡 ${c.name} blames ${o.name} for a suspiciously missing sausage roll.`)}}
function update(dt){clock+=dt;year=Math.floor(clock/5);for(const c of clans)clanTick(c,dt);for(const a of agents)updateAgent(a,dt);agents=agents.filter(a=>a.alive);buildings=buildings.filter(b=>b.hp>0);resources=resources.filter(r=>r.n>0);if(resources.length<160&&Math.random()<dt*2){let wf=.72*regen.food,ww=.14*regen.wood,ws=.14*regen.stone,sum=wf+ww+ws;if(sum>0){let q=Math.random()*sum,kind=q<wf?'food':q<wf+ww?'wood':'stone';resources.push({x:R(30,WORLD.w-30),y:R(30,WORLD.h-30),kind,n:R(8,25)})}}for(const f of fx){f.x+=f.vx*dt;f.y+=f.vy*dt;f.life-=dt}fx=fx.filter(f=>f.life>0);if(Math.random()<dt*.18)battleDetect();if(Math.random()<dt*(chaos?.035:.007))randomEvent();if(Math.floor(clock*2)%12===0)updateTerritory();headlineTimer-=dt;if(headlineTimer<=0)$('headline').classList.remove('on');if(director){let hot=battles.sort((a,b)=>b.life-a.life)[0];if(hot&&Math.random()<dt*.9){cam.tx=hot.x;cam.ty=hot.y;cam.tz=1.25}else if(Math.random()<dt*.04){let hero=agents.filter(a=>a.alive).sort((a,b)=>b.fame-a.fame)[0];if(hero&&hero.fame>4){cam.tx=hero.x;cam.ty=hero.y;cam.tz=1.05}}}cam.x+=(cam.tx-cam.x)*dt*2.4;cam.y+=(cam.ty-cam.y)*dt*2.4;cam.z+=(cam.tz-cam.z)*dt*2.4}
function drawTerrain(){if(terrainCanvas)ctx.drawImage(terrainCanvas,0,0);else{ctx.fillStyle='#355744';ctx.fillRect(0,0,WORLD.w,WORLD.h)}}
function drawTerritory(){for(const c of clans){ctx.save();ctx.globalAlpha=.08;ctx.fillStyle=c.color;for(const b of buildings){if(b.c!==c.id||b.hp<=0)continue;ctx.beginPath();ctx.arc(b.x,b.y,b.type==='castle'?180:110,0,7);ctx.fill()}ctx.restore()}}
function drawRoads(){ctx.save();ctx.lineCap='round';for(const c of clans){const home=buildings.find(b=>b.c===c.id&&b.type==='castle');if(!home)continue;for(const b of buildings){if(b.c!==c.id||b===home)continue;ctx.strokeStyle='rgba(54,43,30,.28)';ctx.lineWidth=8/cam.z;ctx.beginPath();ctx.moveTo(home.x,home.y);ctx.quadraticCurveTo((home.x+b.x)/2+Math.sin(b.x*.02)*30,(home.y+b.y)/2+Math.cos(b.y*.02)*30,b.x,b.y);ctx.stroke();ctx.strokeStyle='rgba(206,178,120,.32)';ctx.lineWidth=3/cam.z;ctx.stroke()}}ctx.restore()}
function drawResource(r){ctx.save();ctx.translate(r.x,r.y);if(r.kind==='food'){ctx.fillStyle='#f3c94e';ctx.strokeStyle='#6d5420';ctx.lineWidth=1.2/cam.z;ctx.beginPath();ctx.arc(0,0,6,0,7);ctx.fill();ctx.stroke();ctx.fillStyle='#fff1a5';ctx.fillRect(-1.3,-5,2.6,10);ctx.fillRect(-5,-1.3,10,2.6)}else if(r.kind==='wood'){ctx.fillStyle='#62472e';ctx.fillRect(-2,2,4,7);ctx.fillStyle='#5ca65b';for(const q of [[0,-3],[-4,1],[4,1]]){ctx.beginPath();ctx.arc(q[0],q[1],5,0,7);ctx.fill()}}else{ctx.fillStyle='#9fa7ad';ctx.strokeStyle='#646d73';ctx.lineWidth=1/cam.z;ctx.beginPath();ctx.moveTo(-7,5);ctx.lineTo(-4,-4);ctx.lineTo(2,-7);ctx.lineTo(7,-1);ctx.lineTo(5,6);ctx.closePath();ctx.fill();ctx.stroke()}ctx.restore()}
function drawBuilding(b){let c=clans[b.c];ctx.save();ctx.translate(b.x,b.y);let lv=b.level||1;ctx.scale(1+(lv-1)*.12,1+(lv-1)*.12);ctx.lineWidth=2.2/cam.z;ctx.strokeStyle='rgba(25,22,18,.8)';const roof=c.color;if(b.type==='castle'){ctx.fillStyle='#c7b998';ctx.fillRect(-18,-12,36,25);ctx.fillRect(-23,-19,11,32);ctx.fillRect(12,-19,11,32);ctx.fillStyle=roof;ctx.fillRect(-23,-23,11,6);ctx.fillRect(12,-23,11,6);ctx.fillRect(-18,-17,36,7);ctx.strokeRect(-18,-12,36,25);ctx.fillStyle='#3a2e25';ctx.fillRect(-4,2,8,11)}else if(b.type==='farm'){ctx.fillStyle='#9a7d41';ctx.fillRect(-16,-12,32,24);ctx.strokeStyle='rgba(245,220,132,.7)';for(let y=-8;y<=8;y+=5){ctx.beginPath();ctx.moveTo(-13,y);ctx.lineTo(13,y);ctx.stroke()}ctx.fillStyle=roof;ctx.fillRect(-5,-15,10,6)}else if(b.type==='barracks'){ctx.fillStyle='#bba781';ctx.beginPath();ctx.moveTo(-15,10);ctx.lineTo(0,-14);ctx.lineTo(15,10);ctx.closePath();ctx.fill();ctx.stroke();ctx.fillStyle=roof;ctx.fillRect(-2,-15,4,25)}else if(b.type==='hospital'){ctx.fillStyle='#e7e2d3';ctx.fillRect(-13,-11,26,22);ctx.strokeRect(-13,-11,26,22);ctx.fillStyle='#d94949';ctx.fillRect(-3,-8,6,16);ctx.fillRect(-8,-3,16,6);ctx.fillStyle=roof;ctx.fillRect(-13,-15,26,5)}else if(b.type==='watchtower'){ctx.fillStyle='#8a6b45';ctx.fillRect(-5,-13,10,26);ctx.fillRect(-12,-18,24,8);ctx.strokeRect(-12,-18,24,8);ctx.fillStyle=roof;ctx.beginPath();ctx.moveTo(-14,-18);ctx.lineTo(0,-27);ctx.lineTo(14,-18);ctx.closePath();ctx.fill()}else{ctx.fillStyle='#b8a784';ctx.fillRect(-10,-9,20,18);ctx.fillStyle=roof;ctx.beginPath();ctx.moveTo(-12,-9);ctx.lineTo(0,-18);ctx.lineTo(12,-9);ctx.closePath();ctx.fill()}if(b.maxHp&&b.hp<b.maxHp*.75){ctx.fillStyle='rgba(20,20,20,.65)';ctx.fillRect(-15,18,30,3);ctx.fillStyle=b.hp<b.maxHp*.35?'#ff665c':'#ffd05b';ctx.fillRect(-15,18,30*clamp(b.hp/b.maxHp,0,1),3)}ctx.restore()}
function drawAgent(a){let c=clans[a.c],moving=Math.hypot(a.vx,a.vy)>.5,step=moving?Math.sin(clock*9+a.x*.045)*1.6:0;ctx.save();ctx.translate(a.x,a.y);ctx.globalAlpha=.22;ctx.fillStyle='#000';ctx.beginPath();ctx.ellipse(0,11,7,3,0,0,7);ctx.fill();ctx.globalAlpha=1;ctx.fillStyle='#2b211d';ctx.fillRect(-5,6+step*.25,4,7);ctx.fillRect(1,6-step*.25,4,7);ctx.fillStyle=c.color;ctx.strokeStyle='rgba(20,18,16,.92)';ctx.lineWidth=1.4/cam.z;ctx.beginPath();ctx.roundRect(-6,-3,12,11,3);ctx.fill();ctx.stroke();ctx.fillStyle='#33271f';ctx.fillRect(-6,4,12,2);ctx.fillStyle=c.color;ctx.beginPath();ctx.roundRect(-9,-1+step*.15,4,9,2);ctx.roundRect(5,-1-step*.15,4,9,2);ctx.fill();ctx.stroke();ctx.fillStyle='#e7b98d';ctx.beginPath();ctx.arc(0,-8,5.1,0,7);ctx.fill();ctx.stroke();ctx.fillStyle=a.role==='guard'||a.role==='heavy'?'#68727c':a.role==='raider'?'#4b342b':a.role==='builder'?'#f2bc49':'#392a22';if(a.role==='guard'||a.role==='heavy'){ctx.beginPath();ctx.arc(0,-9,5.2,Math.PI,Math.PI*2);ctx.lineTo(5,-7);ctx.lineTo(-5,-7);ctx.closePath();ctx.fill();ctx.stroke()}else if(a.role==='builder'){ctx.fillRect(-6,-12,12,2.8);ctx.fillRect(-4,-14,8,2.5)}else{ctx.beginPath();ctx.arc(0,-10,4.6,Math.PI,Math.PI*2);ctx.fill()}ctx.lineCap='round';ctx.lineJoin='round';if(a.role==='guard'||a.role==='leader'){ctx.strokeStyle='#e8eef2';ctx.lineWidth=2.4/cam.z;ctx.beginPath();ctx.moveTo(7,1);ctx.lineTo(13,-9);ctx.stroke();ctx.strokeStyle='#7e5b32';ctx.lineWidth=3/cam.z;ctx.beginPath();ctx.moveTo(6,2);ctx.lineTo(8,-1);ctx.stroke();ctx.fillStyle=c.color;ctx.strokeStyle='#e4d7b0';ctx.lineWidth=1.4/cam.z;ctx.beginPath();ctx.arc(-8,2,5,0,7);ctx.fill();ctx.stroke()}if(a.role==='raider'){ctx.strokeStyle='#6c4a2b';ctx.lineWidth=2.8/cam.z;ctx.beginPath();ctx.moveTo(7,5);ctx.lineTo(12,-8);ctx.stroke();ctx.fillStyle='#d9e0e4';ctx.strokeStyle='#333';ctx.lineWidth=1/cam.z;ctx.beginPath();ctx.moveTo(10,-10);ctx.lineTo(16,-9);ctx.lineTo(15,-4);ctx.lineTo(11,-5);ctx.closePath();ctx.fill();ctx.stroke()}if(a.role==='heavy'){ctx.strokeStyle='#765331';ctx.lineWidth=2.5/cam.z;ctx.beginPath();ctx.moveTo(7,7);ctx.lineTo(15,-13);ctx.stroke();ctx.fillStyle='#e8eef2';ctx.beginPath();ctx.moveTo(15,-16);ctx.lineTo(18,-10);ctx.lineTo(13,-12);ctx.closePath();ctx.fill()}if(a.role==='scout'){ctx.strokeStyle='#d5a657';ctx.lineWidth=1.7/cam.z;ctx.beginPath();ctx.arc(9,0,6,-1.2,1.2);ctx.stroke();ctx.strokeStyle='#eee';ctx.lineWidth=.8/cam.z;ctx.beginPath();ctx.moveTo(11,-5);ctx.lineTo(11,5);ctx.stroke()}if(a.role==='builder'){ctx.strokeStyle='#6e4c2e';ctx.lineWidth=2.4/cam.z;ctx.beginPath();ctx.moveTo(7,6);ctx.lineTo(10,-5);ctx.stroke();ctx.fillStyle='#8b949b';ctx.fillRect(7,-7,8,3)}if(a.role==='worker'){ctx.fillStyle='#b58645';ctx.strokeStyle='#5f4328';ctx.lineWidth=1/cam.z;ctx.beginPath();ctx.roundRect(6,2,7,7,2);ctx.fill();ctx.stroke()}if(a.role==='medic'){ctx.fillStyle='#f5f5ef';ctx.strokeStyle='#333';ctx.lineWidth=1/cam.z;ctx.beginPath();ctx.roundRect(6,0,8,8,2);ctx.fill();ctx.stroke();ctx.fillStyle='#d84747';ctx.fillRect(9,-1,2,10);ctx.fillRect(6,3,8,2)}if(a.role==='leader'||a.fame>8){ctx.strokeStyle='#ffe18a';ctx.lineWidth=1.8/cam.z;ctx.beginPath();ctx.arc(0,0,15,0,7);ctx.stroke()}if(cam.z>1.15){ctx.fillStyle='#fff';ctx.strokeStyle='#000';ctx.lineWidth=3/cam.z;ctx.font=`900 ${8/cam.z**.08}px system-ui`;ctx.textAlign='center';ctx.strokeText(ROLEICON[a.role]||'•',0,-17);ctx.fillText(ROLEICON[a.role]||'•',0,-17)}ctx.restore()}
function draw(){ctx.setTransform(DPR,0,0,DPR,0,0);ctx.fillStyle='#061018';ctx.fillRect(0,0,W,H);ctx.save();ctx.translate(W/2,H/2);ctx.scale(cam.z,cam.z);ctx.translate(-cam.x,-cam.y);drawTerrain();drawTerritory();drawRoads();for(const r of resources)drawResource(r);ctx.globalAlpha=1;for(const b of buildings)drawBuilding(b);for(const a of agents)drawAgent(a);for(const b of battles){ctx.globalAlpha=clamp(b.life/b.max,0,1)*.65;ctx.strokeStyle='#fff';ctx.lineWidth=3/cam.z;ctx.beginPath();ctx.arc(b.x,b.y,22+(b.max-b.life)*8,0,7);ctx.stroke()}ctx.globalAlpha=1;for(const f of fx){ctx.globalAlpha=f.life/f.max;ctx.fillStyle=f.color;ctx.font='bold 15px system-ui';ctx.fillText(f.text,f.x,f.y)}ctx.globalAlpha=1;for(const c of clans){ctx.fillStyle=c.color;ctx.font=`950 ${16/cam.z**.12}px system-ui`;ctx.fillText(`${c.em} ${c.name}`,c.home.x+20,c.home.y-22)}ctx.restore();$('year').textContent='Y'+year;$('pop').textContent=agents.length+' souls';$('wars').textContent=Math.floor(clans.reduce((n,c)=>n+c.war.size,0)/2)+' wars';renderCards()}
function renderCards(){if(!$('modal').classList.contains('open'))return;let total=clans.reduce((n,c)=>n+c.territory,0)||1;$('cards').innerHTML=clans.map(c=>{let alive=agents.filter(a=>a.c===c.id).length,hero=agents.filter(a=>a.c===c.id&&a.alive).sort((a,b)=>b.fame-a.fame)[0];return `<div class="card"><b><i class="dot" style="background:${c.color}"></i>${c.em} ${c.name}</b> — ${c.trait}<div class="mini">Pop ${alive} • Territory ${Math.round(c.territory/total*100)}% • Food ${Math.floor(c.food)} • Morale ${Math.floor(c.morale)}<br>${c.war.size?'⚔ '+[...c.war].map(i=>clans[i]?.name).filter(Boolean).join(', '):'No active war'}${hero&&hero.fame>2?`<br>⭐ ${hero.name} • fame ${Math.floor(hero.fame)}`:''}</div></div>`}).join('')}
function lightning(x,y){let hit=0;for(const a of agents){let d=Math.hypot(a.x-x,a.y-y);if(d<70){a.hp-=R(55,105);if(a.hp<=0){a.alive=false;hit++}}}for(let i=0;i<8;i++)fx.push({x:x+R(-8,8),y:y+R(-50,10),vx:R(-12,12),vy:R(-15,35),life:.55,max:.55,text:'⚡',color:'#fff28a'});say(`⚡ Divine lightning hits ${hit||'no'} unfortunate souls.`,true)}
function quake(){let damaged=0;for(const b of buildings){if(Math.random()<.72){b.hp-=R(35,90);damaged++}}for(const a of agents){a.x=clamp(a.x+R(-45,45),12,WORLD.w-12);a.y=clamp(a.y+R(-45,45),12,WORLD.h-12)}say(`🌋 The ground splits. ${damaged} buildings take damage.`,true);for(let i=0;i<34;i++)burst(R(0,WORLD.w),R(0,WORLD.h),'#c98c57')}
function divineStorm(){for(const a of agents){a.vx+=R(-130,130);a.vy+=R(-130,130);if(Math.random()<.08)a.hp-=R(4,18)}say('🌪 A divine storm scatters armies and civilians alike.',true)}
function feastWorld(){for(const c of clans)c.food+=120;for(let i=0;i<70;i++)resources.push({x:R(30,WORLD.w-30),y:R(30,WORLD.h-30),kind:'food',n:R(18,45)});say('🍗 The heavens open. Every clan receives an absurd feast.',true)}
function growWood(){for(let i=0;i<90;i++)resources.push({x:R(30,WORLD.w-30),y:R(30,WORLD.h-30),kind:'wood',n:R(14,38)});say('🌲 Forests surge across the world overnight.',true)}
function blessWeakest(){let c=clans.slice().sort((a,b)=>a.territory-b.territory)[0];if(!c)return;c.food+=100;c.wood+=70;c.stone+=45;c.morale=100;for(let i=0;i<5;i++)spawnAgent(c.id,c.home.x+R(-55,55),c.home.y+R(-55,55),i<2?'guard':'worker');say(`✨ ${c.name}, currently getting pumped, receives divine favour.`,true)}
function curseLeader(){let c=clans.slice().sort((a,b)=>b.territory-a.territory)[0];if(!c)return;c.morale=Math.max(10,c.morale-35);c.rage=Math.min(100,c.rage+25);if(c.leader&&c.leader.alive)c.leader.hp=Math.max(12,c.leader.hp-55);say(`☠️ ${c.name}'s leading dynasty is cursed. Morale collapses.`,true)}
